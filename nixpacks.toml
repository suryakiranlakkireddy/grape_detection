[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd App && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd App && python manage.py collectstatic --noinput"]

[start]
cmd = "cd App && gunicorn ultimate_ai.wsgi --bind 0.0.0.0:$PORT"
